#pragma once
#include <string>
#include <cmath>
const int BASE_WINDOW_WIDTH = 1000;
const int BASE_WINDOW_HEIGHT = 1000;
const std::string BASE_TITLE = "Tic-Tac-Toe";
const int FRAMERATE_LIMIT = 30;
const float BASE_FONT_SIZE = std::sqrt(BASE_WINDOW_WIDTH * BASE_WINDOW_HEIGHT) * 0.1;